{
  "name": "love-actually",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "cra-template": "1.2.0",
    "react": "18.0.0",
    "react-dom": "18.0.0",
    "react-draggable": "^4.4.6",
    "react-scripts": "5.0.1"
  },
  "homepage": "https://sector47.github.io/love-actually-webmessage",
  "scripts": {
    "predeploy": "npm run build", 
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "echo \"No tests specified\" && exit 0",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "web-vitals": "^4.2.4"
  }
}
